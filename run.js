const { runThis } = require("./src/index.js");

//Manual run:
//Edit and run this by: `nps`
// See: config.js for available options reference.

const args = {
  command: "presort",
  config: "dev"
};

runThis(args);
